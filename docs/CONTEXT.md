# 旅行决策与行程规划领域

本文件统一项目中的业务语言。它只定义概念，不描述技术实现。

## 用户需求

**旅行请求（TripRequest）**：
用户用自然语言表达的原始旅行想法，可能不完整、模糊或相互冲突。
_Avoid_: 完整需求、用户画像

**旅行画像（TripProfile）**：
从旅行请求及后续回答中整理出的当前有效需求集合，会随着对话持续更新。
_Avoid_: 问卷、永久用户画像

**固定事实（FixedFact）**：
用户无法或不应由系统改变的条件，例如出行人数、已购车票和身体限制。
_Avoid_: 普通硬约束

**可协商硬约束（NegotiableHardConstraint）**：
未获得用户同意前必须满足，但无解时可以向用户提出放宽选项的条件，例如预算上限。
_Avoid_: 软偏好、系统自动放宽条件

**软偏好（SoftPreference）**：
用于比较方案优劣、发生冲突时允许牺牲的偏好，例如喜欢美食或尽量少走路。
_Avoid_: 必须条件

## 目的地与资源

**旅行目的地（TravelDestination）**：
用户计划前往并停留的城市或旅行区域，一个目的地内部可以包含多个游玩区域、景点、餐厅和住宿点。
_Avoid_: 单个景点、单个POI

**游玩区域（ActivityArea）**：
目的地内部地理上相近、适合安排在同一时段或同一天的一组游玩地点。
_Avoid_: 旅行目的地、行政区必须一致

**游玩地点（VisitPlace）**：
行程中实际到访的景点、街区、公园、商圈、体验项目或其他活动地点。
_Avoid_: 旅行目的地

**支持目的地（SupportedDestination）**：
系统已有足够数据并允许进入完整行程规划的城市或旅行区域。
_Avoid_: 人工写死的首批名单、模型知道的城市

**知识覆盖（KnowledgeCoverage）**：
知识库和事实库对某个目的地、资源类型及日期范围的资料完整程度，用于判断能否生成经过验证的攻略。
_Avoid_: LLM常识、人工口头声明支持

**规划就绪（PlanningReady）**：
某个目的地已具备生成P0攻略所需的最低数据覆盖；该状态由数据检查得出，不由LLM自行判断。
_Avoid_: 数据库中只出现过名称、模型能回答

**目的地请求（DestinationRequest）**：
用户对某个目的地表达的天数、优先级、必去程度或可放弃程度。
_Avoid_: 景点请求

**旅行分段（TripSegment）**：
完整旅行中位于同一目的地的一段连续停留，包含分配的日期和天数。
_Avoid_: 单日行程、景点

**住宿分段（StaySegment）**：
连续入住同一住宿点或同一住宿区域的一段日期，用于表达中途换住处而不把住宿等同于每天的独立节点。
_Avoid_: 单次回酒店、旅行分段

**资源候选（ResourceCandidate）**：
可用于规划的景点、餐厅、住宿区域或交通选项，已经关联事实、适用条件和证据。
_Avoid_: 推荐结果、搜索结果

**可用状态（AvailabilityStatus）**：
资源在本次旅行日期和条件下的状态，只能是可用、有条件可用、不可用或未知。
_Avoid_: 推荐分数、喜欢程度

**证据（Evidence）**：
支持某项事实或判断的可追溯资料，包含来源和更新时间。
_Avoid_: 模型记忆、无来源常识

**规划事实（PlanningFact）**：
由数据库、MCP工具或已验证Provider提供并转换为统一结构、允许进入规划计算的事实。
_Avoid_: LLM预训练记忆、未经标记的推测

**数据快照（DataSnapshot）**：
某次规划实际使用的一组带时间版本的数据，用于后续复查和刷新。
_Avoid_: 永久真相、当前数据库全部数据

## 行程与变更

**定制可执行旅游攻略（TravelGuide）**：
面向用户的最终旅行成果，包含总览、抵达方式、行前准备、住宿、每日行程、预算备选以及数据来源，并可随计划变化生成新版本。
_Avoid_: 一次性攻略文章、仅有每日时间表的行程计划

**计划节点（PlanNode）**：
行程时间轴上的一个活动、用餐、休息、住宿或地点间移动安排。
_Avoid_: 仅指景点

**移动段（TravelLeg）**：
两个计划节点之间的交通安排，包含方式、耗时和费用。
_Avoid_: 景点、旅行分段

**行程计划（ItineraryPlan）**：
由旅行分段、每日计划、计划节点、移动段、预算和提示共同构成的内部可执行方案，是定制攻略的核心组成部分而非最终全部内容。
_Avoid_: 定制可执行旅游攻略、景点列表

**计划状态（PlanState）**：
当前行程及其版本、锁定节点、已完成节点、冲突和数据快照的集合。
_Avoid_: 仅指LangGraph状态

**锁定节点（LockedNode）**：
因用户确认、已经预约或已经完成而不得被自动修改的计划节点。
_Avoid_: 高分景点、模型偏好节点

**冲突（Conflict）**：
使计划不可执行或存在明显风险的具体问题，必须关联受影响节点和原因。
_Avoid_: 普通建议、模型不喜欢的安排

**修复选项（RepairOption）**：
针对某个冲突提出的最小修改方案，不得未经允许改变固定事实或可协商硬约束。
_Avoid_: 重新生成整份攻略

**局部重规划（LocalReplan）**：
在保留锁定节点和无关安排的前提下，只重新计算受变化影响的行程部分。
_Avoid_: 从头重新生成全部计划
